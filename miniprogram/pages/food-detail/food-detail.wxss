.detail-swiper { height: 440rpx; }
.detail-img { width: 100%; height: 440rpx; }
.store-name { font-size: 36rpx; font-weight: 700; }
.score-text { font-size: 26rpx; color: #C81D2B; font-weight: 600; }
.store-tags { margin: 16rpx 0; }
.store-address { margin-top: 12rpx; font-size: 26rpx; color: #666; justify-content: space-between; }
.address-nav { color: #C81D2B; font-size: 24rpx; }
.store-desc { font-size: 26rpx; color: #666; line-height: 1.8; }
.dish-list { margin-top: 12rpx; }
.dish-item { display: flex; justify-content: space-between; padding: 16rpx 0; border-bottom: 1rpx solid #F0F0F0; }
.dish-item:last-child { border-bottom: none; }
.dish-name { font-size: 28rpx; }
.bottom-bar { position: fixed; bottom: 0; left: 0; right: 0; background: #fff; padding: 16rpx 24rpx; padding-bottom: calc(16rpx + env(safe-area-inset-bottom)); box-shadow: 0 -2rpx 12rpx rgba(0,0,0,0.06); }
